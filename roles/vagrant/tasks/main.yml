- name: setup vagrant
  package:
    name:
      - vagrant
      - virtualbox
      - "linux{{ linux_kernel_version }}-virtualbox-host-modules"
  become: yes
  when: not ansible_check_mode

- name: reload virtualbox
  command: vboxreload
  become: yes
  when: not ansible_check_mode
